## Summary

<!-- What does this PR do? Why is it needed? -->

## Changes

- 

## Testing

- [ ] Contract unit tests pass (`npm run test:unit` in `contracts/`)
- [ ] Backend tests pass (`npm test` in `backend/`)
- [ ] Frontend builds without errors (`npm run build` in `frontend/`)

## Checklist

- [ ] No changes to `HashBet.sol` contract ABI (or migration plan documented)
- [ ] No breaking changes to the `/api` response shapes
- [ ] Environment variable changes reflected in the relevant `.env.example`
- [ ] PR title follows conventional commits format (`feat:`, `fix:`, `chore:`, etc.)
